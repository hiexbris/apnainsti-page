import React from "react";
import ReactDOM from "react-dom/client";
import ApnaInstiPage from "./pages/apnaInsti";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <ApnaInstiPage />
  </React.StrictMode>
);
